name: Build kernel with SUSF and Upload Artifacts

on:
  workflow_dispatch:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          sudo apt update
          sudo apt install -y clang lld cpio python2 llvm git make gcc flex bison bc libssl-dev ccache libncurses5-dev libncursesw5-dev libtinfo5 cmake python3
      - name: Build Project
        run: | 
          git config --global user.email "you@example.com"
          git config --global user.name "kirill1aliuk-github"
          git submodule add https://github.com/rifsxd/KernelSU.git
          
          export KERNEL_ROOT="$(pwd)"
          git clone https://gitlab.com/simonpunk/susfs4ksu -b 1.4.2-kernel-5.4
          
          # cp ./susfs4ksu/kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch ./KernelSU/
          cp ./susfs4ksu/kernel_patches/KernelSU/kernel/sucompat.h ./KernelSU/kernel/
          cp ./susfs4ksu/kernel_patches/50_add_susfs_in_kernel-5.4.patch ./50_add_susfs_in_kernel.patch
          cp ./susfs4ksu/kernel_patches/fs/susfs.c ./fs/
          cp ./susfs4ksu/kernel_patches/include/linux/susfs.h ./include/linux/
          cp ./susfs4ksu/kernel_patches/fs/sus_su.c ./fs/
          cp ./susfs4ksu/kernel_patches/include/linux/sus_su.h ./include/linux/
          
          cd $KERNEL_ROOT/KernelSU && patch -p1 < 10_enable_susfs_for_ksu.patch
          cd $KERNEL_ROOT && patch -p1 < 50_add_susfs_in_kernel.patch
          
          git add .
          git commit -m "Blump up KSU for NonGKI kernel"
          chmod +x ./build.sh
          ./build.sh all renoir
      - name: Compress
        run: tar -czvf build.tar.gz ../out
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: build
          path: build.tar.gz
